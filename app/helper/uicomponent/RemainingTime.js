import React, {Component} from "react";

export const RemainingTime = (time) => {

    // get time 
    // compare with current time
    // return how many days/hours/minutes has left

	return (
        "3 days"
		);
};
